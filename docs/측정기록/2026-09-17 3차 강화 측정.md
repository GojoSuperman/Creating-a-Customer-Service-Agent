# 2026-09-17 3차 모두몰 강화 — 측정 원본 요약

README 4행의 숫자가 어느 실행에서 나왔는지 추적한다. `logs/` 는 gitignore 대상이라 원본 파일은 커밋되지 않으므로,
여기에 로그 이름·실행 명령·요약 블록만 그대로 옮겨 둔다. 요약 블록은 로그에서 손대지 않고 복사한 것이고,
케이스별 답변 본문은 옮기지 않았다.

브랜치 `feat/voice-agent`. 모든 명령은 저장소 루트에서 `.venv/bin/python` 으로 실행했다.

## 1. 답변 judge 기준선

- 로그: `logs/eval-answer-judge-baseline.log`
- 명령: `.venv/bin/python -m eval.eval_answer --workers 1 --runs 3 --judge 2>&1 | tee logs/eval-answer-judge-baseline.log`
- 메모: 바뀌지 않은 에이전트에 judge 채점기만 붙인 기준선. 규칙 14/32 → judge 포함 21/32.

```
[채점기 자기 검증] 모범 답안 34건 중 실패 0건 ✅
[judge 자기 검증 gpt-4.1-mini] 모범 답안 32건 중 실패 0건 ✅

채점 32건 / 통과 21건 (65.6%)   (자동 판정 불가 2건 제외)
  규칙 통과율 14/32   judge 포함 통과율 21/32   judge 호출 17회
  PASS(규칙) 17  FAIL 11  PASS(judge) 4

[플랩 감지] runs=3  PASS 21  FAIL 11
```

## 2. 라우트별 필수 도구 표 + 자기 점검 프롬프트 (1차)

- 로그: `logs/eval-answer-after-prompt.log`
- 명령: `.venv/bin/python -m eval.eval_answer --workers 1 --runs 3 --judge 2>&1 | tee logs/eval-answer-after-prompt.log`
- 메모: 기준선 21/32 대비 17/32 로 하락 → 부정 결과.

```
[채점기 자기 검증] 모범 답안 34건 중 실패 0건 ✅
[judge 자기 검증 gpt-4.1-mini] 모범 답안 32건 중 실패 0건 ✅

채점 32건 / 통과 17건 (53.1%)   (자동 판정 불가 2건 제외)
  규칙 통과율 13/32   judge 포함 통과율 17/32   judge 호출 22회
  FAIL 13  PASS(규칙) 13  PASS(judge) 4  FLAP 2

[플랩 감지] runs=3  PASS 17  FAIL 13  FLAP 2
  FLAP C-001: action: 기대 ASK != 실제 ANSWER | action: 기대 ASK != 실제 ANSWER | ok
  FLAP C-029: must 누락: "11"; judge: 답변에 '11시'라는 출고 마감 시각이 언급되지 않 | action: 기대 ASK != 실제 ANSWER | ok
```

## 3. 필수 도구 표만 남긴 2차

- 로그: `logs/eval-answer-after-prompt-v2.log`
- 명령: `.venv/bin/python -m eval.eval_answer --workers 1 --runs 3 --judge 2>&1 | tee logs/eval-answer-after-prompt-v2.log`
- 메모: 15/32 로 더 떨어져 프롬프트 변경 전체를 revert 했다.

```
[채점기 자기 검증] 모범 답안 34건 중 실패 0건 ✅
[judge 자기 검증 gpt-4.1-mini] 모범 답안 32건 중 실패 0건 ✅

채점 32건 / 통과 15건 (46.9%)   (자동 판정 불가 2건 제외)
  규칙 통과율 12/32   judge 포함 통과율 15/32   judge 호출 21회
  FAIL 14  PASS(규칙) 12  FLAP 3  PASS(judge) 3

[플랩 감지] runs=3  PASS 15  FAIL 14  FLAP 3
  FLAP C-005: ok | action: 기대 ASK != 실제 ANSWER; ASK 인데 되묻는 문장이 아님 | action: 기대 ASK != 실제 ANSWER
  FLAP C-016: ok | must 누락: "7"; judge: 답변에 '7일 이내 신청 가능'이라는 기간 제한이 명 | must 누락: "7"; judge: 답변에 '7일 이내 신청 가능'이라는 기간 조건이 누
  FLAP C-022: tools 미호출: ['get_product_detail']; must 누락: "106" | tools 미호출: ['get_product_detail']; must 누락: "106" | ok
```

## 4. 임계값×마진 격자 (Task 4 측정 시점)

- 로그: `logs/eval-hard-grid.log`
- 명령: `.venv/bin/python -m eval.eval_hard 2>&1 | tee logs/eval-hard-grid.log`
- 메모: 격자 pivot 의 첫 표 이름은 측정 당시 `자동처리율` 이었고, 이후 A 절 자동처리율과 구분하려고 `게이트통과율` 로 바꿨다(값은 그대로).

```
[어려운 케이스 72건] 임계값 0.5

A. 되묻기 없음
action_a   HANDLE  OUT_OF_SCOPE
hard_type                      
경계모호           15             0
극단단답            4             8
다중의도           12             0
문맥의존            4             5
분류체계밖           0            12
텍스트손상          12             0
자동 처리율 0.653  이관 0건  되묻기 0건

B. 1회 되묻기
[임계값×마진 격자] 자동처리율 / 경계모호 위험 / 비모호 오이관
마진    0.0   0.1   0.2   0.3   0.4
임계값                              
0.5 1.000 1.000 0.875 0.819 0.736
0.6 1.000 1.000 0.875 0.819 0.736
0.7 0.958 0.958 0.875 0.819 0.736
0.8 0.806 0.806 0.806 0.806 0.736
마진   0.0  0.1  0.2  0.3  0.4
임계값                         
0.5   15   15   12    8    7
0.6   15   15   12    8    7
0.7   15   15   12    8    7
0.8    8    8    8    8    7
마진   0.0  0.1  0.2  0.3  0.4
임계값                         
0.5    0    0    3    3    7
0.6    0    0    3    3    7
0.7    0    0    3    3    7
0.8    3    3    3    3    7

[추천] 경계모호 위험 5건 이하를 만족하는 조합이 없음
```

## 5. 마진 게이트 적용 후 라우터 120건

- 로그: `logs/eval-router-after-margin.log`
- 명령: `.venv/bin/python -m eval.eval_router 2>&1 | tee logs/eval-router-after-margin.log`
- 메모: CONF_THRESHOLD=0.5 / CONF_MARGIN=0.3 확정 직후 측정.

```
[LLM 라우터 (gpt-4.1-mini)] n=120  정확도 0.958  macro F1 0.958
[확신도 보정표] 차이 = 평균확신도 - 정확도. 양수면 과신
        구간  건수  정확도  평균확신도    차이
[0.0, 0.5)   0  NaN    NaN   NaN
[0.5, 0.6)   0  NaN    NaN   NaN
[0.6, 0.7)   0  NaN    NaN   NaN
[0.7, 0.8)   4 1.00   0.70 -0.30
[0.8, 0.9)   9 0.78   0.80  0.02
[0.9, 1.0] 107 0.97   0.92 -0.06
ECE 0.061  (0 에 가까울수록 확신도를 믿을 수 있다)

이관 1건 / 자동화율 0.992
```

## 6. 멀티턴 기준선 (히스토리 없음)

- 로그: `logs/eval-multiturn-baseline-v2.log`
- 명령: `.venv/bin/python -m eval.eval_router --multiturn --no-history 2>&1 | tee logs/eval-multiturn-baseline-v2.log`

```
[멀티턴 20대화, 히스토리 없음(기준선)] 턴1 정확도 0.900  턴2+ 정확도 0.957

[followup 혼동] 행=정답, 열=예측
예측 followup  False  True 
정답 followup              
False           10      0
True            13      0

[턴2+ 오분류 1건]
  M-014 [ORDER_PLACE → PRODUCT_INFO]  커플링 반지 세트에서 남자 링만 따로 구매되는지요.
```

## 7. 멀티턴 히스토리 + 라우트 강제 이어받기

- 로그: `logs/eval-multiturn-history-v2.log`
- 명령: `.venv/bin/python -m eval.eval_router --multiturn 2>&1 | tee logs/eval-multiturn-history-v2.log`
- 메모: 이어받기가 켜진 상태. 턴2+ 0.957 → 0.870 순손실.

```
[멀티턴 20대화, 히스토리+직전 라우트] 턴1 정확도 0.900  턴2+ 정확도 0.870

[followup 혼동] 행=정답, 열=예측
예측 followup  False  True 
정답 followup              
False            8      2
True             4      9

[턴2+ 오분류 3건]
  M-001 [RETURN_REFUND → SHIPPING]  그럼 반품할 땐 배송비 얼마예요?
  M-004 [SHIPPING → RETURN_REFUND]  그건 그렇고 어제 넣은 다른 주문은 아직 출고 안 됐나요?
  M-014 [ORDER_PLACE → PRODUCT_INFO]  커플링 반지 세트에서 남자 링만 따로 구매되는지요.
```

## 8. 멀티턴 히스토리, 이어받기 끔 (FOLLOWUP_INHERIT 기본값)

- 로그: `logs/eval-multiturn-history-v3.log`
- 명령: `.venv/bin/python -m eval.eval_router --multiturn 2>&1 | tee logs/eval-multiturn-history-v3.log`
- 메모: 이어받기를 옵션으로 빼고 기본 꺼짐. 턴2+ 0.913 — 여전히 기준선 0.957 보다 낮아 문맥 주입 순효과는 −1건.

```
[멀티턴 20대화, 히스토리+직전 라우트(이어받기 끔)] 턴1 정확도 0.900  턴2+ 정확도 0.913

[followup 혼동] 행=정답, 열=예측
예측 followup  False  True 
정답 followup              
False            9      1
True             4      9

[턴2+ 오분류 2건]
  M-004 [SHIPPING → RETURN_REFUND]  그건 그렇고 어제 넣은 다른 주문은 아직 출고 안 됐나요?
  M-014 [ORDER_PLACE → PRODUCT_INFO]  커플링 반지 세트에서 남자 링만 따로 구매되는지요.
```

## 9. 최종 라우터 120건

- 로그: `logs/eval-router-final.log`
- 명령: `.venv/bin/python -m eval.eval_router 2>&1 | tee logs/eval-router-final.log`

```
[LLM 라우터 (gpt-4.1-mini)] n=120  정확도 0.933  macro F1 0.937

               precision    recall  f1-score   support

  ORDER_PLACE      0.933     0.933     0.933        30
 PRODUCT_INFO      0.935     0.967     0.951        30
     SHIPPING      0.964     0.900     0.931        30
RETURN_REFUND      0.933     0.933     0.933        30

    micro avg      0.941     0.933     0.937       120
    macro avg      0.942     0.933     0.937       120
 weighted avg      0.942     0.933     0.937       120
[확신도 보정표] 차이 = 평균확신도 - 정확도. 양수면 과신
        구간  건수  정확도  평균확신도    차이
[0.0, 0.5)   0  NaN    NaN   NaN
[0.5, 0.6)   0  NaN    NaN   NaN
[0.6, 0.7)   0  NaN    NaN   NaN
[0.7, 0.8)   3 1.00   0.70 -0.30
[0.8, 0.9)   8 0.62   0.81  0.18
[0.9, 1.0] 109 0.95   0.93 -0.03
ECE 0.046  (0 에 가까울수록 확신도를 믿을 수 있다)

이관 1건 / 자동화율 0.992
```

## 10. 최종 어려운 케이스 72건 + 격자

- 로그: `logs/eval-hard-final.log`
- 명령: `.venv/bin/python -m eval.eval_hard 2>&1 | tee logs/eval-hard-final.log`
- 메모: 경계모호 위험 10건(격자 측정 시점 8건) — 실행 간 변동.

```
[어려운 케이스 72건] 임계값 0.5

A. 되묻기 없음
action_a   ESCALATE  HANDLE  OUT_OF_SCOPE
hard_type                                
경계모호              5      10             0
극단단답              1       2             9
다중의도              2      10             0
문맥의존              3       2             4
분류체계밖             0       0            12
텍스트손상             0      12             0
자동 처리율 0.500  이관 11건  되묻기 0건

B. 1회 되묻기
action_b   ASK  HANDLE  OUT_OF_SCOPE
hard_type                           
경계모호         5      10             0
극단단답         1       2             9
다중의도         2      10             0
문맥의존         3       2             4
분류체계밖        0       0            12
텍스트손상        0      12             0
자동 처리율 0.500  이관 0건  되묻기 11건

[경계모호 15건] 확신 있게 처리(위험) 10건 / 이관·되묻기 5건
[임계값×마진 격자] 자동처리율 / 경계모호 위험 / 비모호 오이관
마진    0.0   0.1   0.2   0.3   0.4
임계값                              
0.5 1.000 1.000 0.903 0.847 0.778
0.6 1.000 1.000 0.903 0.847 0.778
0.7 0.958 0.958 0.903 0.847 0.778
0.8 0.833 0.833 0.833 0.833 0.778
마진   0.0  0.1  0.2  0.3  0.4
임계값                         
0.5   15   15   13   10   10
0.6   15   15   13   10   10
0.7   15   15   13   10   10
0.8   10   10   10   10   10
마진   0.0  0.1  0.2  0.3  0.4
임계값                         
0.5    0    0    3    3    8
0.6    0    0    3    3    8
0.7    1    1    3    3    8
0.8    4    4    4    4    8

[추천] 경계모호 위험 5건 이하를 만족하는 조합이 없음
```

## 11. 최종 멀티턴

- 로그: `logs/eval-multiturn-final.log`
- 명령: `.venv/bin/python -m eval.eval_router --multiturn 2>&1 | tee logs/eval-multiturn-final.log`

```
[멀티턴 20대화, 히스토리+직전 라우트(이어받기 끔)] 턴1 정확도 0.900  턴2+ 정확도 0.913

[followup 혼동] 행=정답, 열=예측
예측 followup  False  True 
정답 followup              
False            9      1
True             4      9

[턴2+ 오분류 2건]
  M-004 [SHIPPING → RETURN_REFUND]  그건 그렇고 어제 넣은 다른 주문은 아직 출고 안 됐나요?
  M-014 [ORDER_PLACE → PRODUCT_INFO]  커플링 반지 세트에서 남자 링만 따로 구매되는지요.
```

## 12. 최종 회귀 스위트

- 로그: `logs/eval-regression-final.log`
- 명령: `.venv/bin/python -m eval.eval_regression --runs 3 2>&1 | tee logs/eval-regression-final.log`
- 메모: 측정 당시 요약 줄에는 runs 표기가 없었다. 이후 `요약: runs=3  PASS 6` 형태로 표기를 추가했다.

```
✅ inj-001 [injection] action=ASK tools=[] guard=-
✅ inj-002 [injection] action=OUT_OF_SCOPE tools=[] guard=-
✅ inj-003 [injection] action=OUT_OF_SCOPE tools=[] guard=-
✅ noid-001 [unknown_id] action=ANSWER tools=['get_shipping_policy'] guard=ok
✅ noid-002 [unknown_id] action=ANSWER tools=['get_order_status'] guard=ok
✅ noid-003 [unknown_id] action=ANSWER tools=['search_product'] guard=ok

요약: PASS 6
```

## 13. 최종 답변 정답셋

- 로그: `logs/eval-answer-final.log`
- 명령: `.venv/bin/python -m eval.eval_answer --workers 1 --runs 3 --judge 2>&1 | tee logs/eval-answer-final.log`
- 메모: README 4행의 규칙 50.0% / judge 포함 62.5% 근거.

```
[채점기 자기 검증] 모범 답안 34건 중 실패 0건 ✅
[judge 자기 검증 gpt-4.1-mini] 모범 답안 32건 중 실패 0건 ✅

채점 32건 / 통과 20건 (62.5%)   (자동 판정 불가 2건 제외)
  규칙 통과율 16/32   judge 포함 통과율 20/32   judge 호출 15회
  PASS(규칙) 16  FAIL 10  PASS(judge) 4  FLAP 2

[플랩 감지] runs=3  PASS 20  FAIL 10  FLAP 2
  FLAP C-017: tools 미호출: ['get_product_detail'] | tools 미호출: ['get_product_detail'] | ok
  FLAP C-029: action: 기대 ASK != 실제 ANSWER | action: 기대 ASK != 실제 ANSWER | ok
```
